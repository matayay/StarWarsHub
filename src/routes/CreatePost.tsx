import Form from "../components/Form";

const CreatePost = () => {
    return (
        <div className="flex min-h-screen items-center justify-center">
            <Form buttonName="Create Post" />
        </div>
    );
};

export default CreatePost;
